## 0.1.0 (2019-12-08)

### Breaking changes
* None.

### Enchancements
* Added `Fraction` class, including methods `toString()`, `toNumber()`, `add()`, `sub()`, `mul()`, and `div()`.
* Added API documentation.

### Fixes
* None.
